defmodule Retro.RetroReminderTest do
  use ExUnit.Case

  describe "RetroReminder.handle_call/2" do
    test "accepts :remind and sends emails to members that have a retro coming up" do
     #TODO test this
    end
  end
end